# Mac의 Codex로 글 해석하기

[글 해석하기](https://stat-thon.github.io/stock-language/#interpret)는 붙여넣은 주식 글의 핵심 주장, 전문용어, 글에서 제시한 근거와 추정, 확인할 사항을 짧게 설명합니다. 답변은 생성되는 대로 화면에 표시됩니다.

OpenAI API 키를 입력하는 방식이 아닙니다. 이 Mac에 로그인된 Codex의 ChatGPT 사용 권한과 사용 한도를 이용합니다. Codex App Server를 사용하는 개인용 기능이며, 공개된 누구나 사용할 수 있는 분석 서버로 배포하지 않습니다.

## 이 Mac에서 시작하기

현재 작업한 Mac에는 Codex CLI와 ChatGPT 로그인이 준비되어 있습니다. 연결 프로그램은 켜져 있는 동안만 동작합니다.

1. 프로젝트 폴더의 **Start Analysis.command**를 더블 클릭합니다. 터미널 창에 ‘문장 너머 Codex 연결이 켜졌습니다’가 나오면 준비됐습니다.
2. [Mac에서 글 해석하기](http://127.0.0.1:8767/#interpret)를 엽니다. 이 페이지는 같은 사이트의 로컬 사본이며 연결을 자동으로 확인합니다.
3. 본문을 붙여넣고 필요한 경우 출처 주소도 입력한 뒤 **글 해석하기**를 누릅니다. **가상 예시 채우기**로 먼저 확인할 수 있습니다.
4. **중지**를 누르면 현재 요청을 취소합니다. 받은 일부 내용은 화면에 남고 미완료로 표시됩니다. **결과 복사**로 직접 보관할 수 있습니다.
5. 사용을 마치면 연결 프로그램의 터미널에서 **Control+C**를 누릅니다. Mac이 잠자기 상태이거나 프로그램이 꺼져 있으면 새 분석을 할 수 없습니다.

터미널에서 직접 실행해도 됩니다.

```sh
cd /path/to/stock-language
npm run bridge
```

새 컴퓨터에서는 Node.js 24 이상과 Codex CLI를 설치하고, `codex login`으로 ChatGPT에 먼저 로그인하세요. Codex 자체가 정상적으로 작동해야 합니다. 이 기능에는 별도 npm 런타임 패키지가 필요하지 않습니다.

현재 연결 프로그램은 Codex CLI 0.147.0에서 검증했으며, 계정의 사용 가능한 모델 목록을 확인한 뒤 이 분석 요청에만 `gpt-5.5`를 사용합니다. Codex 앱의 기본 모델이나 전역 설정은 바꾸지 않습니다. 해당 모델을 사용할 수 없으면 안내하고 중지하며 API 결제로 전환하지 않습니다.

## GitHub Pages 화면에서 연결하기

1. Mac 연결 프로그램을 켭니다.
2. 공개 사이트의 **글 해석하기 → 이 Mac 연결**을 누릅니다.
3. 새로 열린 로컬 창에서 요청한 주소가 `https://stat-thon.github.io`인지 확인하고 **이 사이트 연결하기**를 누릅니다.
4. 원래 사이트에 **이 Mac의 Codex와 연결됨**이 나타나면 본문을 분석합니다.

브라우저가 팝업 또는 로컬 네트워크 접근을 묻는다면 이 사이트의 연결을 허용해야 합니다. 브라우저 정책 때문에 연결할 수 없으면 **Mac에서 열기**로 로컬 사본을 사용하세요. 현재 연결은 이 컴퓨터의 `127.0.0.1:8767`에만 열립니다. 휴대폰에서 같은 주소를 열면 휴대폰 자신을 가리키므로 이 Mac에 연결되지 않습니다. 휴대폰에서 원격 분석하려면 별도의 인증된 원격 연결 구성이 필요합니다.

연결 권한은 현재 탭의 세션 저장소에 보관하며 개인 학습 기록 백업에는 포함하지 않습니다. 연결 프로그램을 다시 시작하거나 8시간이 지나면 다시 연결하세요. **연결 해제**는 현재 탭의 연결 정보를 지웁니다.

기존 용어·문장 북마크는 사이트 주소별 브라우저 저장소에 있습니다. 로컬 사본과 Pages에서 기존 개인 기록을 함께 쓰려면 ‘내 학습장’의 백업 내보내기/가져오기를 이용하세요.

## 읽을 수 있는 내용과 결과의 범위

- **본문 붙여넣기가 필요합니다.** 출처 URL은 결과에 표시하며 서버가 해당 주소를 가져오거나 토스에 로그인하지 않습니다. 링크만 넣었을 때 본문을 읽은 것처럼 분석하지 않습니다.
- 최대 12,000자를 입력합니다. 한 번에 한 글을 분석하고, 과도한 중복 요청을 막기 위해 Mac 전체에서 1분에 10회로 제한합니다. 이 제한은 Codex 계정의 사용 한도와 별개입니다.
- ‘글에 적혀 있음’과 ‘사실로 검증됨’을 구분합니다. 현재 가격, 실적, 공시를 실시간으로 대조하는 기능은 아닙니다. 분석 중 브라우저·파일·명령 실행 도구를 사용하지 않습니다.
- 통신 오류, 한도 도달, 시간 초과, 중지 때는 일부 결과를 완성된 분석으로 표시하지 않습니다. 최대 90초 후 서버가 요청을 중지합니다.
- 본문과 결과는 이 사이트에 자동 저장·공개하지 않습니다. 화면을 새로고침하면 사라집니다. 모델 처리를 위해 입력이 Codex/OpenAI로 전송되므로 해당 계정의 데이터 처리 정책이 적용됩니다.

## 연결이 되지 않을 때

| 안내 | 확인할 것 |
| --- | --- |
| Mac에 연결하지 못함 | 연결 프로그램의 터미널이 켜져 있는지, Mac이 잠자기 상태인지 확인합니다. 브라우저가 차단하면 ‘Mac에서 열기’를 사용합니다. |
| 8767 포트를 사용 중 | 먼저 실행한 연결 프로그램이 있는지 확인하고 중복 실행하지 않습니다. |
| Codex 로그인 필요 | 터미널에서 `codex login status`를 확인하고 `codex login`으로 ChatGPT에 로그인합니다. API 키 로그인 상태는 이 분석기에서 사용하지 않습니다. |
| 연결 승인 만료 | 공개 사이트에서 다시 ‘이 Mac 연결’을 누릅니다. |
| Codex 사용 한도 | Codex의 사용량과 갱신 시각을 확인합니다. 구독 한도를 우회하거나 API 결제로 자동 전환하지 않습니다. |
| 요청이 많음 | 잠시 기다렸다 다시 실행합니다. 로컬 요청 횟수 제한입니다. |
| 도구 실행 환경을 제한할 수 없음 | Codex CLI 버전과 연결 프로그램을 함께 점검합니다. 제한을 해제해 실행하지 않습니다. |

## 구현과 검증

- `interpret.js`, `interpret.css`: 연결 승인, 입력 검사, 스트리밍 표시, 중지·복사 UI.
- `bridge/server.mjs`: localhost 전용 HTTP 서버, 사이트 origin·Host 검사, 일회성 연결 승인, 세션 토큰, SSE, 본문·시간·동시 실행 제한.
- `bridge/codex-client.mjs`: Codex App Server의 stdio 프로토콜, ChatGPT 인증 확인, 새로운 임시 분석 세션, 도구 접근 제한과 취소 처리.

계정 인증 파일이나 API 키를 브라우저·GitHub·사이트 HTML에 넣지 않습니다. 연결 승인은 임시 권한이며, 웹페이지에 Codex의 전체 원격 제어 인터페이스를 노출하지 않습니다. 서버는 사이트 HTML 한 파일만 제공하고 저장소·홈 디렉터리 파일을 제공하지 않습니다. 서버는 입력 본문·결과·인증정보를 로그에 출력하지 않습니다.

```sh
npm run build
npm test
```

자동 검증에는 가짜 Codex 응답을 사용하므로 실제 구독 사용량이 차감되지 않습니다. 실제 계정의 짧은 분석은 구현 검증 시 별도로 확인합니다. GitHub Actions는 데이터와 계산, 연결 서버·Codex 프로토콜의 격리된 테스트를 실행한 뒤 `index.html`만 Pages에 배포합니다. Mac 연결 프로그램은 GitHub Pages 서버에서 실행되지 않습니다.

기반 기능과 지원 상태: [Codex App Server](https://learn.chatgpt.com/docs/app-server), [ChatGPT/Codex 인증](https://learn.chatgpt.com/docs/auth). App Server는 버전에 따라 인터페이스가 달라질 수 있으며 원격 WebSocket 연결은 실험적입니다. 이 프로그램은 Mac 내부의 stdio 연결을 사용합니다.
