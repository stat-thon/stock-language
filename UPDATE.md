# 자료를 계속 업데이트하는 방법

사이트 하단의 **자료 업데이트**에서 같은 절차와 관련 링크를 볼 수 있습니다.

## 가장 간단한 방법: Codex에 업데이트 요청

이 프로젝트에서 아래처럼 요청하세요. 사이트에서도 요청문을 복사할 수 있습니다.

> SK하이닉스, 삼성전자, 대한광통신 커뮤니티를 실제 화면에서 확인해 문장 너머 자료를 업데이트해줘. 새로운 종목도 필요하면 추가해줘. 용어를 배울 수 있는 글은 본문 전체를 읽고, 문맥을 담은 자체 요약과 학습에 필요한 짧은 원문 인용을 구분해서 작성해줘. 공유 버튼에서 개별 글 주소를 얻은 뒤 같은 본문인지 다시 확인하고, 작성자와 확인일을 남겨줘. 기존 글·논점의 중복, 광고, 욕설 중심 글은 제외해줘. 용어 뜻은 공식 자료와 대조하고 원문의 주장과 해석을 구분해줘. cases.json, glossary.json, meta.json과 수집 메모를 보강하고 검증한 다음 GitHub Pages에 반영해줘.

정기 점검 안내가 표시되면 종료 시각 뒤에 다시 확인합니다. 2026-09-13 한국시간 00시경 ‘23:55–00:10’ 점검 안내를 확인했고 00:10 이후 정상적으로 읽었습니다. 점검 중에는 이미 읽은 자료의 해석·출처 확인을 진행할 수 있습니다.

새 글을 가져오는 작업과 새 자료를 배포하는 작업은 별개입니다. 현재 커뮤니티 자동 수집 API는 연결되어 있지 않습니다. 별도의 Codex 예약 작업이 새 글 확인·선별·배포를 수행하도록 등록되어 있습니다. 이는 웹페이지에 내장된 수집 API와는 별개이며, 실행 여부와 결과는 Codex의 예약 작업에서 확인합니다. 저장소의 `main`에 자료를 반영하면 검증·HTML 생성·Pages 배포가 자동 실행됩니다.

## 직접 읽은 글을 보태기

1. 사이트의 **문장 추가**에서 종목, 짧은 발췌, 출처 URL, 직접 쓴 해석을 저장합니다. 기존 용어집에 있는 용어가 들어간 문장을 추가할 수 있습니다.
2. **자료 업데이트 → 추가한 사례 내보내기**로 공개 반영을 검토할 제안 JSON을 내려받습니다. 이 파일에는 직접 추가한 사례와 필요한 추가 종목만 담깁니다. 북마크·학습 완료·복습 기록은 담기지 않습니다.
3. 파일을 이 Codex 작업에 첨부하고 “출처와 해석을 검토해서 공개 자료에 반영해줘”라고 요청합니다. 내보낸 문장·해석 자체에 공개하면 안 되는 내용이 없는지도 확인합니다.
4. 실제 출처를 다시 확인하고, 해석·출처 범위·확인 질문을 보완한 사례만 공개 자료에 반영합니다. 파일을 내려받는 것만으로 사이트가 바뀌지는 않습니다.

개인 기록은 접속한 브라우저에 저장되며 다른 기기로 자동 동기화되지 않습니다. localhost에서 사용하던 기록을 Pages로 옮기려면 기존 사이트의 **내 학습장 → 백업 내보내기** 후 Pages에서 **백업 가져오기**를 사용하세요. 전체 개인 백업 파일은 공개 GitHub 저장소에 올리지 마세요.

## 소스 자료 편집

| 파일 | 내용 |
| --- | --- |
| `cases.json` | 글 전체의 맥락 요약, 짧은 실제 발췌, 해석, 용어 ID, 개별 출처·수집일, 확인 질문 |
| `glossary.json` | 중복을 정리한 용어, 쉬운 정의, 예시, 오해, 공식 근거 링크 |
| `meta.json` | 종목 목록, 수집 범위·방법, 한계, 사이트 링크 |
| `collection-notes.json` | 원문을 확인하면서 정리한 짧은 발췌와 맥락 메모 |
| `analysis.json` | 기술적 분석·기업 분석·투자 대가의 관점 수업, 가상 기업 분석 순서, 공식 근거 |

GitHub에서 파일을 열고 연필 버튼으로 편집할 수 있습니다. 새 사례의 `id`는 기존과 겹치지 않게 지정하고, `stock`은 `meta.json`의 6자리 종목 코드, `terms`는 `glossary.json`의 용어 ID를 사용하세요. 동일 용어가 나와도 새로운 해석상의 도움이 있으면 사례로 남길 수 있고, 같은 논점만 반복하면 제외합니다.

새 공개 사례는 글의 공유 버튼에서 개별 링크를 얻고, 단축 주소를 열어 `/community/posts/…`로 이동한 뒤 최종 본문과 발췌가 일치하는지 확인합니다. `sourceUrl`에는 개별 주소, `sourceShareUrl`에는 확인한 공유 주소, `sourceAuthor`에는 표시 작성자, `sourceVerifiedAt`에는 다시 읽은 날짜를 남깁니다. 주소를 추측하거나 종목 피드를 개별 글 링크처럼 표시하지 않습니다. 개별 주소를 찾지 못한 기존 사례는 그 한계를 `sourceNote`에 남기며, 재확인하지 않은 본문을 전체 맥락 요약으로 보완하지 않습니다.

본문을 끝까지 읽은 사례는 `postSummary`에 글의 배경·주장·조건·근거의 범위를 자신의 말로 정리합니다. 로그인 등으로 일부만 확인했다면 `postSummaryScope: "partial"`로 표시하고 확인하지 못한 범위를 설명합니다. 전체를 확인했을 때만 `full`을 사용하거나 이 선택 필드를 생략합니다. `explanation`은 발췌의 전문용어 풀이이므로 두 필드를 구분합니다. 제3자의 글 전문은 공개 자료에 복제하지 않고, `excerpt`에는 학습에 필요한 짧은 인용을 보존합니다. 같은 원글에서 다른 논점을 다룬 기존 사례는 같은 `samePostGroup`을 명시하고 모든 발췌의 합계를 공백 기준 25단어 이내로 유지합니다. 정확한 게시 시각을 모르면 화면의 상대시간을 그대로 남기고 수집일과 구분합니다. 원문 수치나 전망은 대조하지 않았으면 미검증임을 명시합니다.

사이트에서 추가한 개인 기록은 검토 전 자료입니다. 이를 그대로 `cases.json`에 복사하지 말고 공개 사례 형식에 맞춰 검토·보완하세요. 새 종목은 `meta.json`에 등록하면 사이트의 종목 목록에 나타납니다.

## 투자분석 수업 보강

투자분석 탭을 늘릴 때는 `analysis.json`의 `lessons`에 수업을 추가합니다. 기존 수업과 겹치지 않는 ID, 분야, 제목, 수준, 요약, 용어별 뜻·계산식·예시·한계, 가상 표현, 해석 순서, 반박 질문, 확인한 공식 출처를 포함하세요. 실제로 읽지 않은 문장을 커뮤니티 글이나 투자 대가의 직접 발언으로 표시하지 않습니다. 실제 발췌는 기존 `cases.json` 절차로 수집하고 수업에서 연결합니다.

차트는 `analysis-charts.js`의 결정적인 가상 데이터와 `analysis-math.js`의 계산을 사용합니다. 새 차트 유형을 추가하면 수치 표와 기본·반례 설명을 함께 제공하세요. 지표 설정, 평균 방식, 단위와 기간을 명시하고 미래 가격을 알고 있는 듯한 설명을 피합니다. 재무 예시는 세전·세후, 평균·기말, 연결 범위, 주식수·금액 단위를 맞춰 확인합니다.

종합 실습의 재무자료를 바꾸면 `analysis.js`의 표시 자료, `analysis-math.js`의 계산, `analysis.json`의 실습 문장과 검증 예제를 함께 갱신합니다. 일반적인 CFO−CAPEX 잉여현금흐름과 가치평가에 사용하는 정상화 FCFF의 정의를 구분합니다. 새 게시물이 없는 날에도 투자분석 수업을 임의로 최신 자료처럼 표시할 필요는 없습니다.

## 로컬 검증과 배포

Node.js 24 이상에서 별도 패키지 설치 없이 실행합니다.

```sh
npm run build
node verify-analysis-math.mjs
```

개인 제안 파일을 점검하려면 다음 명령을 사용합니다. 공개 자료를 자동으로 수정하지 않습니다.

```sh
node import-contributions.mjs /path/to/contributions.json
node import-contributions.mjs /path/to/contributions.json --write --out /path/to/review.json
```

검증은 형식·중복·용어 연결·수업 구조를 확인합니다. 계산 검증은 알려진 값과 대조하고 DCF의 단위·민감도, 무효 가정, 차트의 가격 범위를 확인합니다. 글의 진위나 해석의 정확성까지 판단하지는 못하므로 원문과 공식 자료를 사람이 대조해야 합니다.

앱 기능을 수정한 경우에는 브라우저 회귀 검증도 실행할 수 있습니다. 설치된 macOS Chrome을 사용하며, 다른 환경에서는 Playwright Chromium을 설치하세요.

```sh
npm ci
npx playwright install chromium
npm test
```

변경한 소스와 생성된 `index.html`을 저장소의 `main`에 커밋·푸시합니다. GitHub Actions의 **Validate and deploy Pages**가 성공하면 같은 사이트 주소에 반영됩니다. 열어 둔 사이트는 새로고침해서 최신 자료를 읽으세요. Pull Request에서는 검증과 빌드만 실행됩니다. 검증에 실패하면 새 배포는 실행되지 않고 기존 배포가 유지됩니다. 재배포가 필요하면 Actions에서 **Run workflow**를 사용할 수 있습니다.

GitHub 웹에서 JSON을 직접 편집한 경우 `index.html`을 따로 수정할 필요는 없습니다. 배포 과정에서 새 HTML을 생성합니다. 소스 저장소의 `index.html`은 다음 로컬 빌드 때 갱신되므로, 최신 배포본은 Pages에서 확인하세요.

오류가 있는 자료를 올렸다면 해당 커밋을 되돌리고 다시 `main`에 반영하세요. 이전 버전도 Git 기록에 남습니다.

배포 구성은 [GitHub Pages 공식 문서](https://docs.github.com/en/pages/getting-started-with-github-pages/using-custom-workflows-with-github-pages)를 따릅니다.

## 개인용 글 해석 기능 업데이트

화면은 `interpret.js`·`interpret.css`, Mac 연결은 `bridge/`를 수정합니다. [BRIDGE.md](BRIDGE.md)의 실행·지원 범위와 함께 갱신하세요. `npm test`는 실제 모델을 호출하지 않는 프로토콜·서버 테스트와 브라우저 회귀 검증을 포함합니다. 인증·도구 제한을 바꿀 때는 허용되지 않은 도구가 없는지 실패 시 차단되는지 함께 검증합니다.

Pages 배포에는 `index.html`만 포함됩니다. 연결 프로그램을 업데이트한 뒤에는 Mac에서 실행 중인 프로그램을 Control+C로 끄고 다시 실행해야 새 코드가 적용됩니다. 일반 커뮤니티 자료 보강과 매일 예약 수집은 글 해석 연결을 사용할 필요가 없습니다.
